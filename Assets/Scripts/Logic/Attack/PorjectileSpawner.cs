using System;
using UnityEngine;

public class PorjectileSpawner : MonoBehaviour
{
    [SerializeField] GameObject projectile;
    [SerializeField] float spawnRate = 2f;
    [SerializeField] float timer = 0;
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
